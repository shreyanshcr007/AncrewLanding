import React from "react";

export const Services = (props) => {
  return (
    <div id="services" className="text-center">
      <div className="container">
        <div className="section-title">
          <h2>Our Services</h2>
          <p>
          Our company offers services like Cloud and Platform Modernization, SaaS Product and Platform Development, Cloud Native Application Development & Testing, Security as a Service (SECaaS), Cloud Security Engineering, and Cloud Governance & Reporting, ensuring scalable, secure, and efficient cloud solutions for modern businesses
          </p>
        </div>
        <div className="row">
          {props.data
            ? props.data.map((d, i) => (
                <div key={`${d.name}-${i}`} className="col-md-4">
                  {" "}
                  <i className={d.icon}></i>
                  <div className="service-desc">
                    <h3>{d.name}</h3>
                    <p>{d.text}</p>
                  </div>
                </div>
              ))
            : "loading"}
        </div>
      </div>
    </div>
  );
};
